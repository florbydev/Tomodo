import ListView from "./ListView"
import SessionCounter from "./SessionCounter"

const Root = () => {
  return (
    <div>Root</div>
  )
}

const TomodoSessionScreen = {
  Root: Root,
  ListView: ListView,
  SessionCounter: SessionCounter
}

export default TomodoSessionScreen